import React, { FC } from "react";

export const RegionForm: FC = () => {
    return (
        <form action="">
            <button>oui</button>
            <button>non</button>
        </form>
    );
};